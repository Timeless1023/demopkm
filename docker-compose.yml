version: '3.8'
services:
  pokemon-trading:
    build: .
    container_name: pokemon-trading-mvp
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - POKEMON_DB_PATH=/app/data/pokemon_trading.db
